# Delivery App Sample

Minimal HTML/CSS/JS project demonstrating:

* Sign-in page (`index.html`)
* Delivery order page (`delivery.html`) with start/drop-off and description
* Checkout page (`checkout.html`) with payment form
* Shared stylesheet (`style.css`) and script (`script.js`)

Open `index.html` in a browser to test.
